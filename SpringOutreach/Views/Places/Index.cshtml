@model IEnumerable<SpringOutreach.Models.Place>

@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <div class="row">
        <div class="col">
            <h1>All Institutions</h1>
        </div>
        <div class="col ms-auto">
            <p>
                <a asp-action="Create" class="btn btn-outline-dark float-end">Add Institution</a>
            </p>
        </div>
    </div>
</div>

<div class="table-responsive">
    <table id="mainTable" class="table table-striped table-hover">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Contact.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.City)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Canton)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Outreaches)
                </th>
                <th class="no-sort"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr id="index_td_block">
                    <td>
                        <a asp-action="Details" asp-route-id="@item.Id">
                            @Html.DisplayFor(modelItem => item.Name)
                        </a>
                    </td>
                    <td>
                        <a asp-action="Details" asp-route-id="@item.Id">
                            @Html.DisplayFor(modelItem => item.Contact.Name)
                        </a>
                    </td>
                    <td>
                        <a asp-action="Details" asp-route-id="@item.Id">
                            @Html.DisplayFor(modelItem => item.City)
                        </a>
                    </td>
                    <td>
                        <a asp-action="Details" asp-route-id="@item.Id">
                            @Html.DisplayFor(modelItem => item.Canton)
                        </a>
                    </td>
                    <td>
                        @foreach (var year in item.Outreaches)
                        {
                            <a asp-action="Details" asp-route-id="@item.Id">
                                @Html.DisplayFor(modelItem => year.Year)
                            </a>
                        }
                    </td>
                    <td id="index_td" class="no-sort">
                        <a asp-action="Edit" asp-route-id="@item.Id" class="fa-regular fa-pen-to-square text-dark"></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>