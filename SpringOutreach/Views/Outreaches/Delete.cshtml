@model SpringOutreach.Models.Outreach

@{
    ViewData["Title"] = "Delete";
}

<div class="text-center">
    <h2>Do you want to delete the Outreach @Model.Year from the institution @Model.Place.Name</h2>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Yes" class="btn btn-outline-danger" /> -
        <a asp-action="Index" class="btn btn-outline-dark">No</a>
    </form>
</div>